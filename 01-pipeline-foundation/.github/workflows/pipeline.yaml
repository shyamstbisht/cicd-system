name: Basic CI Pipeline

on:
  push:
    branches:
      - main
      - dev

jobs:
  check:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: List files
        run: ls -la

      - name: Echo message
        run: echo "Pipeline is running"